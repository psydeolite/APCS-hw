public int sum67(int[] nums) {
  boolean sect=false;
  int sum=0;
  for (int i=0;i<nums.length;i++) {
  if (nums[i]==6) {
  sect=true;
  } 
  if (!sect) {
  sum+=nums[i];
  }
  if (nums[i]==7) {
  sect=false;
  }
  }
  return sum;
}

